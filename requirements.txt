# Root convenience requirements only.
#
# Each roadmap project owns its real dependencies in its own pyproject.toml/uv.lock.
# Use this file only for a lightweight root dev shell or quick docs/dashboard work.

fastapi>=0.111.0
httpx>=0.27.0
openai>=1.35.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pytest>=8.0.0
rich>=13.0.0
ruff>=0.4.0
truststore>=0.9.1
uvicorn[standard]>=0.30.0
